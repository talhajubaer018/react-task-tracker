import React from 'react'

const Header = ({}) => {
    return (
        <header>
            <h1>hello</h1>
        </header>
    )
}

// header.defaultProps = {
//     title: 'Task Tracker',
// }

export default Header
